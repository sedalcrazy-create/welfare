# 🎯 سیستم مدیریت مهمانان - شروع سریع

## ⚡ نصب در 3 مرحله

### 1️⃣ Docker را اجرا کنید
- Docker Desktop را باز کنید
- منتظر بمانید تا آماده شود (آیکن سبز)

### 2️⃣ اسکریپت نصب را اجرا کنید
```bash
# Windows
.\setup-guest-system.bat

# Linux/Mac
chmod +x setup-guest-system.sh
./setup-guest-system.sh
```

### 3️⃣ باز کنید و تست کنید
- URL: http://localhost:8080
- ایمیل: admin@bankmelli.ir
- رمز: password

رفتن به: **پرسنل** → انتخاب یک پرسنل → تب **"مهمانان"**

---

## 📚 مستندات

| فایل | محتوا |
|------|-------|
| `SETUP_INSTRUCTIONS.md` | 📖 راهنمای کامل نصب و عیب‌یابی |
| `GUEST_SYSTEM_GUIDE.md` | 🔧 راهنمای استفاده و API |
| `GUEST_SYSTEM_IMPLEMENTATION.md` | 📊 گزارش کامل پیاده‌سازی |
| `PERSONNEL_GUESTS_SPEC.md` | 📐 مشخصات فنی سیستم |

---

## 🎨 ویژگی‌های کلیدی

✅ **مهمان یکتا** - بر اساس کد ملی
✅ **اشتراک‌گذاری** - یک مهمان با چند پرسنل
✅ **دسته‌بندی خودکار** - بانکی (6 نوع) / متفرقه (3 نوع)
✅ **لیست قابل ویرایش** - اضافه/حذف برای هر سفر
✅ **رابط کاربری Ajax** - بدون رفرش صفحه

---

## 🚀 تست سریع

```bash
# ساخت داده نمونه (اختیاری)
docker-compose exec app php artisan db:seed --class=GuestTestSeeder

# تست در Tinker
docker-compose exec app php artisan tinker
>>> $p = \App\Models\Personnel::first()
>>> $p->guests
```

---

## 🆘 مشکل دارید؟

```bash
# بررسی وضعیت
docker-compose ps

# مشاهده لاگ‌ها
docker-compose logs app

# راه‌اندازی مجدد
docker-compose down
docker-compose up -d
```

---

**✅ همه چیز آماده است - فقط Docker را روشن کنید و اسکریپت را اجرا کنید!**

💡 برای جزئیات بیشتر: `SETUP_INSTRUCTIONS.md`
